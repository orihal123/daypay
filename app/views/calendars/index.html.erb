<%= render partial: 'layouts/header' %>

<%= month_calendar(attribute: :date) do |date| %>
  <%= date.day %><br>
  <p class="text-primary"><%= @calendar_data[date]%><br></p>
  <%= @calendar_daydata[date]%><br>
  <%= @calendar_budgets[date] %> <!-- カレンダー内に日付ごとの予算を表示 -->
  
<% end %>


<%= render partial: 'layouts/footer' %>